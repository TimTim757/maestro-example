# Copy to .env.local and adjust (do not commit .env.local).
# Scripts load .env.local automatically via scripts/env.sh.

# Password used in scenario 01 (import) and scenario 02 (login)
TEST_PASSWORD=TestPassword123!

# 12-word test-only seed — use quotes (spaces between words)
TEST_SEED_PHRASE="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"
